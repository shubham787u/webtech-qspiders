public class DivisibleBy {
    public static void main(String[] args) {
        //16)Given three number print them in sorted order(ascending)
    


        // int n = 30;
        // String result = (n%3==0 && n%5==0) ? "Divisible by both 3 and 5" :(n%3 ==0) ? "It is divisible by 3" : (n %5==0) ? "It is divisible only 5" : " Divisible by None";
        // System.err.println(result);
    }
}
